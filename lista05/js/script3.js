let numero = parseInt(prompt("digite um valor para saber sua tabuada:"))
for(let contadora = 1; contadora <11;contadora++){
{
 console.log(numero + " X " + contadora+ " = " + numero+ contadora)
    }
}